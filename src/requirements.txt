# Single flat requirements file for the one Pants resolve this project uses
# (runtime + test + lint/typecheck tooling together), rather than splitting
# runtime/dev/test across several files that all have to stay in sync by
# hand. Run `make lock` (== `pants generate-lockfiles`) after editing this
# file.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0

# Settings / validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Testing
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2

# Lint / format / typecheck
black==24.8.0
isort==5.13.2
flake8==7.1.1
mypy==1.11.2
